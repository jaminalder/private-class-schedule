# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
#GET     /

GET     /api/todo/all         controllers.Application.todos
POST    /api/todo/save        controllers.Application.addTodo
POST    /api/todo/save/:id    controllers.Application.updateTodo(id)
POST    /api/todo/delete/:id  controllers.Application.deleteTodo(id)

# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /*file               controllers.Assets.at(path="/public/angular-frontend/app", file)
